export default function Projects() {
    const styles = {
        text: {
            fontSize: 'clamp(0.8rem, 1.5vw, 1.25rem)',
            color: '#ff789f',
        },
        
    };

    return (
        <div id="projects" className="info-container">
            <h1 className="section-title">Projects</h1>
          
        </div>
    );
}
